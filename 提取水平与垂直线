图像形态学操作的时候，可以通过自定义的结构元素实现结构元素对输入图像一些对象敏感，另外一些不敏感，这样就会让敏感对象改变而不敏感对象保留输出。
通过使用两个最基本的形态学操作——膨胀与腐蚀，使用不同结构元素实现对输入图像的操作，得到想要的结果。

结构元素
  
提取步骤
  输入图像彩色图像 imread
  转换为灰度图像 cvtColor
  转换为二值图像 adaptiveThreshold
  定义结构元素
  开操作（腐蚀+膨胀）提取 水平与垂直线
  
  
